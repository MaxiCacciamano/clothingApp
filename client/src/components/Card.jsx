import React from 'react'
import { useSelector } from 'react-redux'

export const Card = () => {
  // const {men} = useSelector(state => state)
  return (
    <div>
      {/* {
        men.map((e)=>(
          name: e.name
        ))
      } */}
    </div>
  )
}
